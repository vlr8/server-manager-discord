# ======================================================================
# server-manager-discord: consolidated requirements
#
# Only DIRECT dependencies are listed here. Transitive deps (numpy,
# scipy, tokenizers, etc.) are pulled in automatically by pip.
#
# DO NOT run `pip freeze > requirements.txt` and commit the output.
# It dumps 150+ transitive packages including nvidia-cuda-* bloat
# and a PyPI package called "common" that CONFLICTS with our
# common/ package directory.
#
# To add a new dep: add the package name here, pip install it,
# and test. Do not pin transitive deps.
# ======================================================================

# === Core (all 3 bots) ===
discord-py-interactions>=5.15.0
aiohttp>=3.12
python-dotenv>=1.0
emoji>=2.0

# === Persona bot: Claude API ===
anthropic>=0.76

# === Persona bot: RAG pipeline ===
sentence-transformers>=5.0
chromadb>=1.4

# === Persona bot: Image handling ===
pillow>=11.0

# === Protector bot: Content analysis ===
vaderSentiment>=3.3
beautifulsoup4>=4.14
lxml>=6.0

# === Trannyverse bot: Analytics & charts ===
pandas>=2.3
matplotlib>=3.10
seaborn>=0.13
tinydb>=4.7

# === Data import scripts (large JSON streaming) ===
ijson>=3.4